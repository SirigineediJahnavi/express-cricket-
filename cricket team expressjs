// app.js

const express = require("express");
const app = express();
module.exports = app;

// Other configurations or middleware setups could go here

// Assuming there's a port defined
const PORT = 3000;

// Listen to the defined port
app.listen(PORT, () => {
  console.log(`Server is running on port ${PORT}`);
});
